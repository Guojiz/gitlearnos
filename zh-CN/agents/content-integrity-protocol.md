# 内容完整性协议

使用这个协议，避免把笔记和来源混在一起。

## 标签

- `source`：完整材料
- `summary`：压缩说明
- `index`：仅用于导航
- `model`：可复用模型
- `todo`：待补内容

## 检查

使用文件前，先问：

1. 它完整吗？
2. 它是原始材料，还是只是摘要？
3. 它是否链接到来源？
4. 它适合公开吗？
5. 下一步要做什么？

## 规则

如果来源缺失，把它标记为 `todo`。
