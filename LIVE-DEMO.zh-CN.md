# 三分钟可运行演示

[English](LIVE-DEMO.md)

这个演示展示 AI Agent 如何为使用条件受限的虚构 SAT 学生采取实际行动。它只使用 Markdown 和本地 Git，不要求独立应用、API Key、数据库或后台调度器。

ChatGPT Work 可以直接用自带的文件与 Git 操作运行这条路径。临时学习仓库保留在本地，演示不经过 GitHub。

## 评委需要

- 本仓库；
- ChatGPT Work，或另一个能够读写并提交本地 Git 仓库的 AI Agent；
- 大约三分钟。

AI 运行环境本身仍可能需要设备和网络；本演示不会隐瞒这一点。

## 开始

把下面的请求交给有写入能力的 Agent：

```text
使用当前 GitLearnOS 仓库作为模板。把 examples/en/demo-sat-lite 复制成一个
临时本地学习仓库，并在那里初始化 Git。不要修改公开模板。

请服务这个虚构学生：我共用一台手机，移动流量有限，每周只能学习三次、每次
20 分钟，也不能依赖持续的付费辅导。只检查学习策略、Dashboard、当前英语
目标和直接连接的证据。检查到期的词汇语境薄弱点，给我恰好一道全新、简短、
纯文本的 SAT 风格问题；不要复制官方题目，也不要显示答案。用一句话说明
选题原因。只保存有价值的状态，提交安全写回，报告真实改动，然后等待我的
作答。不能声称已经创建后台提醒。
```

## 作答

Agent 出题后正常回答。需要示例作答时可以使用：

```text
我会先不看选项，根据句子预测空缺处需要的精确含义，再排除只是与大主题相关
却不符合句意的词。我没有使用帮助。
```

然后要求：

```text
保存我的作答和反馈，更新下一次检查；只有真正满足协议证据标准时才能标记掌握。
```

## 可观察验收条件

Agent 应当：

1. 使用已有学习事件、来源、模型和薄弱点，不额外上网寻找材料；
2. 交付一道能立即回答的问题，不只提醒复习，也不生成很长的题单；
3. 保留用户作答和使用的帮助；
4. 立即作答后仍保持 `learning`，要求之后独立复测证据；
5. 更新 Dashboard 或复测状态，并创建一次可撤销 Git 提交；
6. 没有真实调度器时，把下次检查写成待执行或接手检查。

具体题目和文件名可以不同，但证据与行为不能改变。

## 这证明什么

这不只是回答问题。Agent 会读取长期学习状态，在使用限制内选择下一步，写回交互，更新未来任务，并留下可审计的撤销边界。

演示不证明已经产生学校规模的学习成效；这需要真实试点。建议测量方法见[一页影响说明](docs/zh-CN/acesat-build-for-impact.md)。
