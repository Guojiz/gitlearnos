interface:
  display_name: "GitLearnOS"
  short_description: "Turn learning evidence into models and timed checks"
  default_prompt: "Use $gitlearnos to handle this learning event through authorized Git state, stable knowledge points, verified RAG retrieval, evidence-linked synthesis, and recurring learning schedules."

policy:
  allow_implicit_invocation: true
